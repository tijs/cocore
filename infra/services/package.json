{
  "name": "@cocore/services",
  "version": "0.1.0",
  "private": true,
  "description": "Single-process orchestrator: Firehose + AppView indexer + AppView API + Exchange. Used by docker-compose for local end-to-end testing.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/main.ts",
    "test": "vitest run",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "@cocore/appview": "workspace:*",
    "@cocore/exchange": "workspace:*",
    "@cocore/o11y": "workspace:*",
    "@cocore/sdk": "workspace:*",
    "@effect/platform": "^0.96.2",
    "better-sqlite3": "^11.5.0",
    "effect": "^3.21.4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.5.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.4"
  }
}

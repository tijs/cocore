/**
 * HttpOnly app session cookie. The token maps server-side to a DID backed by a
 * stored AT Proto OAuth session (see integrations/auth/atproto.server.ts).
 */
export const AUTH_SESSION_TOKEN_COOKIE = "cocore-auth.session_token";
